# Tabular Storage System

- Stores full tables in a single DB row
- Uses delimiter-based encoding
- SQLAlchemy backend
- Streamlit UI

## Run
pip install -r requirements.txt
streamlit run app/streamlit_app.py
